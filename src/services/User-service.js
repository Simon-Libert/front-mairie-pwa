/* import axios from 'axios';

const API_URL = `${process.env.REACT_APP_API_URL}`;

const getPublicContent = () => {
	return axios.get(API_URL + 'users/admin/:userId');
};

export default getPublicContent; */
